<%= form_with(model: mining_type) do |form| %>
  <% if mining_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining_type from being saved:</h2>
      <ul>
        <% mining_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <form class="row g-3">
    <div class="col-md-3">
      <%= form.label :description, style: "display: block", class: "form-label"  %>
      <%= form.text_field :description, class:"form-control" %>
    </div>
    <div class="col-md-3">
      <%= form.label :acronym, style: "display: block", class: "form-label"  %>
      <%= form.text_field :acronym, class:"form-control" %>
    </div>
    <br>
    <div class="col-md-3">
      <%= form.submit "#{t('links.confirm')}" , class: "btn btn-primary" %>
      <%= link_to "#{t('links.cancel')}", mining_types_path, class: "btn btn-primary" %>
    </div>
  </form>
<% end %>
